package com.school.student;

/**
 * Complete the TODO markers so that each branch returns the expected value.
 */
public class Conditionals {

    /**
     * Returns "positive", "negative", or "zero" based on the provided number.
     */
    public String describe(int number) {
        // TODO: implement conditional logic
        return "TODO";
    }

    /**
     * Returns the larger of two integers. If both are the same, return either value.
     */
    public int max(int left, int right) {
        // TODO: replace the placeholder return with the correct comparison
        return 0;
    }

    /**
     * Returns true when the number is in the inclusive range [-10, 10].
     */
    public boolean withinTen(int number) {
        // TODO: replace with a conditional expression that checks the range
        return false;
    }
}
