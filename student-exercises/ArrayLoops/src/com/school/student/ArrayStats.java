package com.school.student;

/**
 * Complete each method using loops. Replace the placeholder values with a real implementation.
 */
public class ArrayStats {

    /**
     * Returns the sum of all numbers in the array.
     */
    public int sum(int[] numbers) {
        // TODO: use a loop to add each element
        return 0;
    }

    /**
     * Returns the average as a double. Treat an empty array as 0.0.
     */
    public double average(int[] numbers) {
        // TODO: accumulate values and divide by the length, guarding against zero
        return 0.0;
    }

    /**
     * Returns the maximum value. For an empty array, return Integer.MIN_VALUE.
     */
    public int max(int[] numbers) {
        // TODO: iterate through numbers and track the maximum encountered
        return Integer.MIN_VALUE;
    }
}
